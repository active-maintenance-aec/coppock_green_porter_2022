# Publication PDFs are not redistributed here; the README links to the DOIs.
coppock_green_porter_2022.pdf
coppock_green_porter_2022_preprint.pdf
coppock_green_porter_2022_appendix.pdf

# Quarto build artifacts
/.quarto/
*_files/

# Working log stays private, per the house rule for public repos.
notes.md

# Quarto copies referenced resources beside the PDF; the PDF embeds them.
report/maintained/

# The deposited archive is fetched from Dataverse by download_original.R, not redistributed.
original/

**/*.quarto_ipynb

# RStudio project state
.Rproj.user/

# Remastered edition: NOT PUBLIC. Alex, 2026-08-10: "not ready for the
# remasters to be public." The whole build directory stays out of the repo,
# which is public on GitHub. Do not remove this rule to "make the build
# reproducible" without asking: publishing a corrected edition of a coauthored
# paper is a decision that belongs to Alex and inherits the coauthor
# notification deferred until the corpus settles.
remaster/
.DS_Store
